spring:
  cloud:
    config:
      enabled: false #關掉 config server追蹤
      
      
      
#eureka client
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/ #註冊服務到 Eureka
    healthcheck:
      enabled: true
      
      
      