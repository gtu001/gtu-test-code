	function getToday(){
		var d = new Date();
	    var rtnStr = ('0' + d.getFullYear()).slice(-4) + //
	    ('0' + d.getMonth()).slice(-2) + //
	    ('0' + d.getDate()).slice(-2) + //
	    "";
	    return rtnStr;
	}